import React, {FunctionComponent} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const LibraryPage: FunctionComponent<Props> = (props) => {
    return (
        <div>
            Library
        </div>
    );
};

export default LibraryPage;
