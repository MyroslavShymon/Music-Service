export enum RoutesConstants {
    MAIN = "/main",
    TESTSTART = "/test",
    TEST = "/test/:id",
    LOGIN = "/login",
    REGISTRATION = "/registration",
    SEARCH = "/search",
    GENRE = "/genre",
    LIBRARY = "/library",
}