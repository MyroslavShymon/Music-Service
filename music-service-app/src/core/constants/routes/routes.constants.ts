export enum RoutesConstants {
    MAIN = "/main",
    TEST = "/test/:id",
    LOGIN = "/login",
    REGISTRATION = "/registration",
    SEARCH = "/search",
    GENRE = "/genre",
    LIBRARY = "/library",
}